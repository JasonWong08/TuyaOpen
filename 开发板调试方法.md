可以直拉取我目前工作的GitHub仓库：
https://github.com/JasonWong08/TuyaOpen/tree/cursor/-bc-f540916f-0d05-42a7-82a0-8fb81f5a0dd1-dd02
当前工作分支是：cursor/-bc-f540916f-0d05-42a7-82a0-8fb81f5a0dd1-dd02
我的所有代码（包括前几天的）都已同步更新到这个分支了
环境搭建参考这个文档即可：
https://tuyaopen.ai/zh/docs/quick-start/enviroment-setup
# 使用以下命令进入TuyaOpen目录
cd TuyaOpen
＃使用以下命令激活 tos.py
. ./export.sh

S3 板子，上传验证方法：
在终端进到这个目录：/home/jasonw/Projects/TyOpen_Jason/TuyaOpen/apps/tuya.ai/your_chat_bot/
使用命令 tos.py build 编译代码
用USB数据线连好开发板，确认 AI core 的拨动开关处于供电状态， LED 灯亮起，使用命令查看端口： ls -l /dev/ttyAC*
编译完成后 按住boot 键后点击 reset，进入下载模式。使用命令tos.py flash -p /dev/ttyACM0
上传固件后，可以使用命令查看串口打印信息：tos.py monitor -p /dev/ttyACM0

C3板子，上传验证方法：
由于按复位按钮后，串口端口号会改变，所以需要写个bash脚本，修改一下终端的 .bashrc文件，加入以下脚本：
# ─── TuyaOpen 工具链 ────────────────────────────────────────────────────────
#
# 背景：conda 初始化会把 Python 3.12 (或其他版本) 置入 PATH 最前端。
# ESP-IDF 的 detect_python.sh 会选中这个 Python，然后去寻找对应版本的
# idf5.4_py3.12_env，而实际安装的是 idf5.4_py3.10_env，导致构建失败。
#
# 永久修复策略：
#   1. tyopen 函数：先退出所有 conda 环境，再激活 TuyaOpen venv，
#      确保 venv 使用系统 Python 3.10。
#   2. util.py (platform/ESP32/tools/util.py)：在调用 esp-idf/export.sh
#      之前，从 os.environ 中移除 .venv 和 conda 路径，防止 Cursor / 脚本
#      调用时继承污染环境。

_TYOPEN_ROOT="/home/jasonw/Projects/TyOpen_Jason/TuyaOpen"

tyopen() {
    # 退出所有 conda 环境层（包括 base），避免 Python 版本冲突
    if command -v conda &>/dev/null; then
        while [ -n "${CONDA_PREFIX}" ]; do
            conda deactivate 2>/dev/null || break
        done
    fi

    # 若旧 .venv 由 conda Python 创建（版本不是 3.10），先删除让其重建
    local venv_dir="${_TYOPEN_ROOT}/.venv"
    if [ -d "${venv_dir}" ]; then
        local venv_py
        venv_py=$("${venv_dir}/bin/python3" --version 2>/dev/null | awk '{print $2}')
        local sys_py
        sys_py=$(python3 --version 2>/dev/null | awk '{print $2}')
        if [ -n "${venv_py}" ] && [ "${venv_py}" != "${sys_py}" ]; then
            echo "[tyopen] .venv Python (${venv_py}) != system Python (${sys_py}), recreating..."
            rm -rf "${venv_dir}"
        fi
    fi

    source "${_TYOPEN_ROOT}/export.sh"
}

# 激活 ESP-IDF 环境（需先执行 tyopen）
tyidf() {
    if [ -z "${OPEN_SDK_ROOT}" ]; then
        echo "[tyidf] Run 'tyopen' first to set up TuyaOpen environment."
        return 1
    fi
    export IDF_TOOLS_PATH="${_TYOPEN_ROOT}/platform/ESP32/.espressif"
    source "${_TYOPEN_ROOT}/platform/ESP32/esp-idf/export.sh"
}

# 启动串口监控（用法：tymonitor 或 tymonitor /dev/ttyACM0）
tymonitor() {
    local port="${1:-$(ls /dev/ttyACM* 2>/dev/null | head -1)}"
    if [ -z "$port" ]; then
        echo "[ERROR]: No /dev/ttyACM* device found. Please connect the device."
        return 1
    fi
    if [ -z "${OPEN_SDK_ROOT}" ]; then
        echo "[tymonitor] Run 'tyopen' then 'tyidf' first."
        return 1
    fi
    echo "[INFO]: Monitoring on $port ..."
    cd "${_TYOPEN_ROOT}/platform/ESP32/tuya_open_sdk" && \
    idf.py -p "$port" monitor
}

使用 source ~/.bashrc命令激活以上脚本，或者重新开一个新的终端，使以上脚本生效。

在终端进到这个目录：/home/jasonw/Projects/TyOpen_Jason/TuyaOpen/apps/tuya.ai/petoi_esp32c3_cube/
使用命令 tos.py build 编译代码
用USB数据线连好开发板，需要先按住 Boot按键不放，再按一下Reset按键，再松开Boot按键，这样就可以进下载模式。
用USB数据线连好开发板，使用命令查看端口： ls -l /dev/ttyAC*
编译完成后 使用命令tos.py flash -p /dev/ttyACM0
上传固件后，按一下Reset按键，可以使用命令查看串口打印信息：tymonitor /dev/ttyACM0

